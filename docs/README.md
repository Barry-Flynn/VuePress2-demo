<!-- 
  此页为 /docs/README.md，是访问站点的基础路径时，VuePress文档默认的首页
  为了使多语言文档并列起来结构清晰，我将默认语言也建立了自己的目录，而不是直接放在docs目录
  这也导致此页变得有些多余，因此你可以有以下几种选择，我也列出了各自的优缺点：

  1. 通过JavaScript代码进行跳转，当访问到本页时，会跳转到你设置的默认语言目录的 README.md 首页。
  》优点：本页面无需变动（除非默认语言不是zh），跳转JavaScript代码已经写好，保持现状即可。
  》缺点：访问站点的基础路径（本页面）时有跳转过渡，会造成页面闪烁。

  2. 清空此页内容，将默认语言目录下的 README.md 首页内容全部复制过来，也就是把此页也作为首页。
  》优点：无需动脑子，傻瓜式复制粘贴过来即可。
  》缺点：两页内容完全重复，后期若有修改，需要同时维护两个页面。

  3. 既不使用JavaScript代码直接跳转，也不完全复制默认语言的首页内容，而是自定义本页面。
  》优点：充分发挥你的创意和想法，使你的文档站点首页别具一格。
  》缺点：需要一定的前端开发能力，即便自定义的本页面，也需要提供跳转到默认语言首页的实现方式。
 -->

<script>
  window.location.href = "./zh/"
</script>